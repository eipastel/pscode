---
"@thiagodiogo/pscode": patch
---

When `pscode init --profile dixi` runs, `pscode/config.yaml` is now created with `schema: pstld-workflow` instead of the default `spec-driven`. The success message after init also reflects the actual schema written.
